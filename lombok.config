# Tells Lombok to propagate @Qualifier annotations from fields to generated constructor parameters,
# so Spring can correctly inject named WebClient beans.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
